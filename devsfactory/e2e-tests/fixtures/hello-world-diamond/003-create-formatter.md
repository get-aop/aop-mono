---
title: Implement formatter function
status: PENDING
dependencies: [1]
---

### Description

Create `formatter.ts` with:
1. Import `FormattedGreeting` from `types.ts`
2. Export `formatMessage(message: string): FormattedGreeting` function
3. Function returns `{ message, timestamp: new Date() }`

### Context

Right branch of the diamond. Can run in parallel with 002.

### Result

(filled by agent after completion)

### Review

(filled by reviewer)

### Blockers

(none)
