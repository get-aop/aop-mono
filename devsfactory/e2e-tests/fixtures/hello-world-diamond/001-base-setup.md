---
title: Create project structure and shared types
status: PENDING
dependencies: []
---

### Description

Set up the base project structure:
1. Create `types.ts` with:
   - `GreetingOptions` interface: `{ name: string; uppercase?: boolean }`
   - `FormattedGreeting` type: `{ message: string; timestamp: Date }`

### Context

Base of the diamond - both greet and formatter will depend on these types.

### Result

(filled by agent after completion)

### Review

(filled by reviewer)

### Blockers

(none)
