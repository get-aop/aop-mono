---
title: Implement greeting function
status: PENDING
dependencies: [1]
---

### Description

Create `greet.ts` with:
1. Import `GreetingOptions` from `types.ts`
2. Export `greet(options: GreetingOptions): string` function
3. Function returns "Hello, {name}!" or "HELLO, {NAME}!" if uppercase is true

### Context

Left branch of the diamond. Can run in parallel with 003.

### Result

(filled by agent after completion)

### Review

(filled by reviewer)

### Blockers

(none)
