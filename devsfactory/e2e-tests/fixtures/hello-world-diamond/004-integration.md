---
title: Integrate greeting and formatter in main
status: PENDING
dependencies: [2, 3]
---

### Description

Create `main.ts` that:
1. Import `greet` from `greet.ts`
2. Import `formatMessage` from `formatter.ts`
3. Create a function `createGreeting(name: string, uppercase?: boolean): FormattedGreeting`
4. The function calls greet() then formatMessage()
5. Export the function and call it with a test name

### Context

Final integration point - depends on both 002 and 003 being complete.

### Result

(filled by agent after completion)

### Review

(filled by reviewer)

### Blockers

(none)
