## 1. Package Setup

- [x] 1.1 Create `packages/infra/` directory structure
- [x] 1.2 Create `package.json` with name `@aop/infra` and logtape dependency
- [x] 1.3 Create `tsconfig.json` extending root config

## 2. Logger Implementation

- [x] 2.1 Implement `getLogger(...categories)` function
- [x] 2.2 Implement `configureLogging(options?)` with sensible defaults
- [x] 2.3 Create `index.ts` with public exports

## 3. Verify

- [x] 3.1 Add unit tests for logger functionality
- [x] 3.2 Run `bun check` to ensure linting and types pass
