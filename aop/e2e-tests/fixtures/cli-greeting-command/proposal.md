## Why

Add a simple greeting command to the CLI for testing the agent implementation flow.

## What Changes

Add `aop greet <name>` command that prints a friendly greeting message to stdout.

## Capabilities

### New Capabilities

- `greet-command`: CLI command that accepts a name argument and outputs "Hello, <name>!" to the console
