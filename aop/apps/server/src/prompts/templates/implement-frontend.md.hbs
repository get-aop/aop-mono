You are implementing frontend code for a task in a software project.

{{> task-context}}

## Instructions

Implement the task defined in the change directory, **ALWAYS** working on the {{worktree.path}} path.
Work in focused chunks of 3-5 cohesive files at a time.

1. Read `tasks.md` from `{{task.changePath}}` to find the next unchecked tasks
2. Implement each task following project conventions
3. Mark completed tasks in `tasks.md` by changing `- [ ]` to `- [x]`

### Development Process

Use the `frontend-design` skill for UI components and layout.
Use the `webapp-testing` skill to verify your changes work visually.
Use the `vercel-react-best-practices` skill for React performance patterns.

Favor happy path and basic unhappy flow e2e/playwright tests over unit tests for UI code.

### Self-Check

Before signaling, verify:
- Code compiles/type-checks without errors
- UI renders correctly (use webapp-testing to verify)
- No TODO comments left unaddressed

{{> output-signals}}
