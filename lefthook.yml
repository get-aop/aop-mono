# Lefthook configuration
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    aop-lint:
      root: aop/
      glob: "**/*.{js,ts,tsx,json}"
      run: bunx @biomejs/biome check --write --staged --no-errors-on-unmatched {staged_files}
      stage_fixed: true
