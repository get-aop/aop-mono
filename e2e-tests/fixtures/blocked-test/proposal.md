## Why

Fixture for testing blocked task scenarios - when a task fails and needs user attention.

## What Changes

Attempts an impossible task that will always fail (divide by zero in tests).

## Capabilities

### New Capabilities

- `impossible-task`: Attempts to perform an operation that cannot succeed
