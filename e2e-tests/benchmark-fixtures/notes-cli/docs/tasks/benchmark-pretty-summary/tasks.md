## Tasks

- [ ] Add a new `renderPrettyReport` renderer in the report layer with focused unit tests
- [ ] Keep the existing `renderPlainReport` export and behavior intact while adding the new output mode
