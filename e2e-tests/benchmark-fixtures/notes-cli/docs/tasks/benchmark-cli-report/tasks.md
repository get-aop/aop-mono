## Tasks

- [ ] Add `--tag <tag>` support to the existing `runCli` flow using the new filtering behavior
- [ ] Add `--format pretty` support to the existing `runCli` flow using the new pretty summary renderer
- [ ] Add CLI integration tests for plain, filtered, and pretty output
